{
  "version": "1.0.0",
  "name": "remark-lint-rules",
  "private": true,
  "devDependencies": {
    "create-remark-lint-plugin": "^0.1.2",
    "lerna": "^3.13.0"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "release": "lerna publish",
    "create": "create-remark-lint-plugin --mono"
  },
  "workspaces": [
    "packages/*"
  ]
}
